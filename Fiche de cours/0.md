Oui, ta fiche est utile, mais elle est trop “brouillon de terrain” : tu as les captures et les commandes, mais il manque une structure exploitable en examen/prétest. On voit surtout : activation courriel/iDRAC, bureau à distance, logs d’erreurs, journal d’événements client, performance, puis commandes PowerShell côté client. 

Voici une version mieux présentée, prête à remplacer ton `.md` :

````md
# Fiche de cours – Réseau 3 / Prétest C19

## Objectif

Cette fiche regroupe les actions utiles pour le prétest C19 :

- configuration des alertes courriel iDRAC ;
- accès Bureau à distance ;
- consultation des journaux d’événements ;
- activation de la gestion distante Windows ;
- création d’un collecteur de performances.

---

## 1. Configuration des alertes courriel iDRAC

### Paramètres à vérifier

| Élément | Valeur |
|---|---|
| Serveur SMTP | `courrier.csduroy.qc.ca` |
| Port SMTP | `25` |
| Destinataire | adresse courriel fournie dans la requête |
| Type d’alerte | Mémoire / Stockage |

> À confirmer selon l’énoncé : parfois le serveur SMTP peut être indiqué comme `csduroy.qc.ca` ou `courrier.csduroy.qc.ca`.

### Étapes générales

1. Se connecter à l’interface iDRAC.
2. Aller dans la section des alertes / notifications.
3. Configurer le serveur SMTP.
4. Ajouter le destinataire.
5. Activer les alertes liées à :
   - la mémoire ;
   - le stockage ;
   - les disques / RAID.
6. Envoyer une alerte de test.
7. Vérifier la réception du courriel.

### Capture

<img width="913" height="547" alt="image" src="https://github.com/user-attachments/assets/99c5172d-3923-4b26-87b7-06d9b0f14ad7" />

---

## 2. Activation du Bureau à distance

### À vérifier sur le serveur

- Le Bureau à distance est activé.
- Le pare-feu autorise RDP.
- Le port `3389` répond.
- Le compte utilisé est autorisé à se connecter.
- Le serveur est joignable par IP ou par nom DNS.

### Commandes utiles côté serveur

```bat
reg add "HKLM\SYSTEM\CurrentControlSet\Control\Terminal Server" /v fDenyTSConnections /t REG_DWORD /d 0 /f
netsh advfirewall firewall set rule group="Bureau à distance" new enable=Yes
netsh advfirewall firewall set rule group="Remote Desktop" new enable=Yes
````

### Test côté client

```powershell
Test-NetConnection ADRESSE_IP_SERVEUR -Port 3389
```

Résultat attendu :

```text
TcpTestSucceeded : True
```

---

## 3. Filtrer les erreurs depuis le serveur

Objectif : retrouver rapidement les erreurs pertinentes dans l’Observateur d’événements.

### Journaux à consulter

* Journaux Windows > Application
* Journaux Windows > Système
* Journaux Windows > Sécurité
* Services liés à Remote Desktop
* Services liés à DNS, impression, stockage ou RAID selon le cas

### Capture

<img width="1077" height="799" alt="image" src="https://github.com/user-attachments/assets/4ffce67e-8f35-4258-b00e-097690d0fab6" />

---

## 4. Verrouillage après échecs de connexion

Objectif : identifier les traces liées aux échecs d’authentification.

### Points à vérifier

* Échecs de connexion.
* Verrouillage de compte.
* Mauvais mot de passe.
* Tentatives depuis un poste client.
* Nom du compte concerné.
* Adresse source si disponible.

### Capture

<img width="1787" height="858" alt="image" src="https://github.com/user-attachments/assets/d452a321-19f3-4a81-948d-fc7853c23d15" />

---

## 5. Traces de logs des échecs de connexion

### Événements utiles

| ID événement | Signification      |
| ------------ | ------------------ |
| 4625         | Échec de connexion |
| 4624         | Connexion réussie  |
| 4740         | Compte verrouillé  |
| 4771         | Échec Kerberos     |
| 4776         | Échec NTLM         |

### Capture

<img width="1668" height="978" alt="image" src="https://github.com/user-attachments/assets/2cfc3496-8406-423a-b254-b28c31f664b2" />

---

## 6. Journal d’événements client

Objectif : consulter les événements d’un poste client à distance depuis le serveur.

### Prérequis

Sur le poste client :

* le pare-feu doit autoriser la gestion distante ;
* WinRM doit être activé ;
* les règles RPC / DCOM / Event Log doivent être actives ;
* le compte utilisé doit avoir les droits nécessaires.

### Capture

<img width="1904" height="978" alt="image" src="https://github.com/user-attachments/assets/2cfc3496-8406-423a-b254-b28c31f664b2" />

---

## 7. Commandes PowerShell côté client

À exécuter sur le poste client avec PowerShell en administrateur.

```powershell
Get-NetFirewallRule | Where-Object {
    $_.DisplayName -like "*journal*" -or
    $_.DisplayName -like "*événement*" -or
    $_.DisplayName -like "*Event Log*" -or
    $_.DisplayName -like "*RPC*" -or
    $_.DisplayName -like "*DCOM*"
} | Enable-NetFirewallRule

Enable-PSRemoting -Force

Set-NetFirewallRule -DisplayGroup "Gestion à distance de Windows" -Enabled True
```

### Version complémentaire pour Windows anglais

```powershell
Set-NetFirewallRule -DisplayGroup "Remote Event Log Management" -Enabled True
Set-NetFirewallRule -DisplayGroup "Windows Remote Management" -Enabled True
Enable-PSRemoting -Force
```

---

## 8. Analyseur de performances

Objectif : créer un ensemble de collecteurs de données personnalisé.

### Nom recommandé

```text
PRETESTC19
```

### Compteurs à ajouter

| Compteur                    | Catégorie       |
| --------------------------- | --------------- |
| `% Temps processeur`        | Processeur      |
| `Écriture disque, octets/s` | Disque physique |
| `% octets dédiés utilisés`  | Mémoire         |

### Étapes

1. Ouvrir `perfmon`.
2. Aller dans **Ensembles de collecteurs de données**.
3. Cliquer sur **Défini par l’utilisateur**.
4. Créer un nouvel ensemble.
5. Choisir **Créer manuellement**.
6. Ajouter les compteurs demandés.
7. Nommer l’ensemble `PRETESTC19`.
8. Démarrer la collecte.
9. Vérifier que les données sont bien générées.

### Capture

<img width="1411" height="917" alt="image" src="https://github.com/user-attachments/assets/01078e88-dbe8-4fa5-9c90-5b77e40a8ec2" />

---

## 9. Sauvegarde des journaux d’événements

### Procédure

1. Créer un dossier sur le bureau :

```text
LOGS
```

2. Ouvrir l’Observateur d’événements.
3. Aller dans :

```text
Journaux Windows > Application
```

4. Cliquer sur **Enregistrer tous les événements sous...**
5. Nommer le fichier :

```text
logs_pretest.evtx
```

6. Enregistrer dans le dossier `LOGS`.

---

## 10. Checklist rapide avant validation

* [ ] Alertes iDRAC configurées.
* [ ] Alertes mémoire activées.
* [ ] Alertes stockage activées.
* [ ] Test MEM0001 effectué.
* [ ] Courriel reçu.
* [ ] Courriel transféré à l’enseignant.
* [ ] Bureau à distance fonctionnel.
* [ ] Réseau serveur fonctionnel.
* [ ] Carte réseau configurée en statique.
* [ ] DNS et suffixe DNS vérifiés.
* [ ] Services demandés arrêtés et désactivés.
* [ ] Utilisateurs déplacés dans les bons groupes.
* [ ] Utilisateurs déplacés dans les bonnes OU.
* [ ] Sauvegarde de l’état du système effectuée.
* [ ] Disque RAID identifié.
* [ ] Test de retrait/remise du disque effectué.
* [ ] RAID reconstruit ou revenu à l’état normal.
* [ ] Logs sauvegardés dans le dossier `LOGS`.
* [ ] Collecteur `PRETESTC19` créé.

```

Point important : garde cette version comme fiche “propre”, et tes captures servent de preuve visuelle. Là, ta fiche devient lisible pour réviser vite avant le prétest.
```
